<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Etudiant">

        <!-- Main activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Add Student activity -->
        <activity
            android:name=".AjoutEleveActivity"
            android:exported="false" />

        <!-- Search Student activity -->
        <activity
            android:name=".RechercheActivity"
            android:exported="false" />

        <!-- Student Details activity -->
        <activity
            android:name=".StudentDetailsActivity"
            android:exported="false" />

        <!-- Modify Student activity -->
        <activity
            android:name=".ModifierEleveActivity"
            android:exported="false" />

        <!-- Calcul Moyenne -->
        <activity
            android:name=".CalculMoyenne"
            android:exported="false" />


    </application>

</manifest>